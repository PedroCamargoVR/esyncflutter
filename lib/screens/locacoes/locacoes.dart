import 'package:flutter/material.dart';

class Locacoes extends StatelessWidget {
  const Locacoes({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "LOCAÇÕES",
        style: TextStyle(fontSize: 42, fontWeight: FontWeight.bold),
      ),
    );
  }
}
