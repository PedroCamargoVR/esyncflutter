import 'package:flutter/material.dart';

class Utilitarios extends StatelessWidget {
  const Utilitarios({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "UTILITÁRIOS",
        style: TextStyle(fontSize: 42, fontWeight: FontWeight.bold),
      ),
    );
  }
}
